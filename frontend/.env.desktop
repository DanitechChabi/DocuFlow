# Build bureau (Electron) : l'API est servie en même-origine par le backend
# Express (SERVE_FRONTEND). URL relative → aucun problème CORS, port indifférent.
# PAS de VITE_GOOGLE_CLIENT_ID → connexion Google désactivée dans l'app de bureau.
VITE_API_URL=/api
